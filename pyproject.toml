[project]
name = "divine-spider-astar"
version = "0.1.0"
description = "A* homework plus independent snake scene renderer"
requires-python = ">=3.10"
dependencies = [
    "pygame-ce",
    "pytest>=9.0.0",
]

[tool.uv]
package = false
